apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: remediation-app
  labels:
    app.kubernetes.io/name: mcp-server
    app.kubernetes.io/part-of: remediation-stack
spec:
  selector:
    app.kubernetes.io/name: mcp-server
  ports:
    - name: mcp
      port: 9000
      targetPort: 9000
      protocol: TCP
